
<%= form_with(model: mebrat_list, local: true) do |form| %>
  <% if mebrat_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mebrat_list.errors.count, "error") %> prohibited this mebrat_list from being saved:</h2>

      <ul>
      <% mebrat_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :mebrat_list_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :mebrat_list_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
